/**
Shared Recharts theming so the dashboard's line/bar/area charts stay visually
consistent without each chart re-typing the same slate palette.
*/

export const CHART_GRID_STROKE = "#e2e8f0";

export const CHART_AXIS_TICK_STYLE = { fill: "#64748b", fontSize: 12 };

export const CHART_AXIS_LINE_STYLE = { stroke: "#cbd5e1" };

export const CHART_TOOLTIP_STYLE = {
  borderRadius: 12,
  border: "1px solid #e2e8f0",
  fontSize: 13,
  boxShadow: "0 4px 16px rgba(15,23,42,0.08)",
};
