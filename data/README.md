# Real household data lives here (gitignored)

Everything in this folder except this README is excluded by `.gitignore`.

The app keeps working data in `localStorage`. Use **Export** in the app to write a
`household.json` snapshot into this folder as a backup, and **Import** to load it back.

Seed/example data in `src/data/seed.ts` is deliberately fake placeholder numbers.
